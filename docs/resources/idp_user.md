---
page_title: "gravitino_idp_user Resource - terraform-provider-gravitino"
subcategory: ""
description: |-
  Manages a built-in IDP user for local authentication.
---

# gravitino_idp_user Resource

Manages a built-in IDP user for local authentication.

## Example Usage

```terraform
# Built-in IDP user for local authentication
resource "gravitino_idp_user" "alice" {
  name     = "alice"
  password = "Passw0rd-Alice12"
  enabled  = true
}

# Disabled user
resource "gravitino_idp_user" "legacy" {
  name     = "legacy_operator"
  password = "Passw0rd-Legacy12"
  enabled  = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The username. Must not contain ':'.

### Optional

- `enabled` (Boolean) Whether the user is enabled. Disabled users cannot authenticate.
- `password` (String, Sensitive) The password (12-64 characters).

### Read-Only

- `groups` (List of String) The groups the user belongs to.
- `id` (String) The username of the built-in IDP user.
